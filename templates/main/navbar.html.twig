<nav>
    <ul>
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            {% if app.user.roles == '["ROLE_ADMIN"]' %}
                <li><a href="{{ path('import_user') }}">Importer un fichier</a></li>
            {% endif %}
        <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>
        <li><a href="{{ path('afficherProfil',{'id' : app.user.id }) }}">Mon Profil</a></li>
        {% endif %}
    </ul>
</nav>